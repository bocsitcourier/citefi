# Current generator inventory — execution status tracked separately

READ-ONLY runtime inventory (verified against current routes/UI; no providers run). Directive: `attached_assets/Pasted--ZERO-EXCEPTION-FULL-GENERATION-EXECUTION-DIRECTIVE-Thi_1789056571882.txt`. Existing matrix caveat: `GENERATION_PIPELINES.md:3-9,64-88` says code existence != live provider/asset E2E and current media is NO-GO.

CUSTOMER GENERATORS / TRANSFORMS
- Articles/batches: UI `app/batches/[id]/*`, `app/client/generate` (referenced by onboarding), content detail `app/content/[id]/page.tsx:432-465`; POST `/api/jobs/batch-submit` (route exists), `/api/articles/[id]/regenerate`, `/api/batches/[id]/regenerate-titles`, `/api/jobs/title-pool` (GET). Queue: `lib/pipeline-worker.ts`, generation orchestrator/enhanced/verified generators; Gemini + OpenAI. Minimal inputs: authenticated team, topic/title pool, article count/batch params, brand/site context; DB/Redis, Gemini/OpenAI, storage, credits required. 
- Article metadata buttons in `app/content/[id]/page.tsx:606-667`: POST `/api/content/[id]/regenerate/{seo-title|meta-description|keywords|slug|faq|hashtags}`; synchronous `lib/seo-regenerator.ts`. Input is existing tenant article ID (some routes accept optional body/context). Gemini/OpenAI + DB required.
- Article formatting/hyperlinks: POST `/api/articles/[id]/reformat`, `/apply-hyperlinks` (route files found by matrix/search), batch repair `/api/batches/[id]/fix-image-captions`; OpenAI/Gemini and deterministic cleaners. Existing article + HTML required.
- Hero/body images: Generate/regenerate buttons on content detail; POST `/api/articles/[id]/regenerate-hero`, `/api/content/[id]/regenerate-hero-image`, `/api/media/[id]/regenerate`, `/api/media/assets/[identity]/regenerate`, `/api/batches/[id]/regenerate-images`; `lib/direct-image-operation.ts`, `lib/gemini-image-generator.ts`, `lib/gemini-social-image-generator.ts`. Gemini image (`gemini-2.5-flash-image` explicitly in `app/api/content/[id]/regenerate-hero-image/route.ts:141`), PNG/Sharp, Spaces/storage, article/media identity and credits required. Image asset regenerate defaults platform `tiktok` (`app/api/media/assets/[identity]/regenerate/route.ts:44-50`). No image edit/inpaint contract.
- Social text/captions/hashtags: UI `app/social/create/page.tsx:304`, content article social buttons `app/content/[id]/page.tsx:1126-1234`; POST `/api/social_posts/generate`, `/api/social-posts/variants/[variantId]/regenerate`; GET `/api/social_posts/[id]`. Supported generator platforms: X, Facebook, Instagram, LinkedIn, Pinterest (`GENERATION_PIPELINES.md:25-27`); article-linked input or topic/copy + platform array; optional `includeImage`. BullMQ `social-post-generation`, Gemini draft then OpenAI enhancement, DB/storage/credits.
- Social image: included in social generation (`includeImage`), may reuse article hero or call Gemini; platform aspect mappings implemented in `lib/gemini-social-image-generator.ts:89-233` (inspect `aspectConfig` there for exact ratios). Existing social post/team and prompt required.
- Slideshow social video: UI `app/social/[id]/page.tsx:378-422`; POST `/api/social/video/generate` (body fields `socialPostId`, `platform` default `tiktok`, `videoType` default `slideshow`, `force`), batch POST `/api/social/video/batch` (`socialPostIds`, platform), cancel POST `/api/social/video/cancel`; status routes include storage-check. Queue worker `lib/social-video-generator.ts`, script `lib/gemini-video-script-generator.ts`, Gemini images + OpenAI TTS + FFmpeg + GPT metadata. Platform route default TikTok; formats MP4; vertical/horizontal composition in `lib/veo-video-generator.ts:348-366` (9:16 => 1080x1920; 16:9 => 1920x1080). Requires existing social posts, FFmpeg, temp/storage, provider keys.
- Idea video: UI `app/social/idea-video/page.tsx:248`; POST `/api/social/video/idea` (create concept), POST `/api/social/video/idea/[id]/generate`; GET/DELETE same ID. Like-this flow: POST `/api/social/video/like`, then `/like/[id]/analyze`, `/like/[id]/generate`; external HTTPS media URL is minimum input. Analyzer is not generation alone. Veo pipeline (`lib/veo-video-generator.ts`, `lib/veo-video-tts-generator.ts`) supports aspect `16:9|9:16`, duration `4|6|8` seconds/clip, TTS voice selected by tone (`lib/veo-video-tts-generator.ts:32,98-146`); output MP4, FFmpeg, Veo/Gemini/OpenAI TTS, storage required.
- Podcast: content detail Generate Podcast button `app/content/[id]/page.tsx:673-707,1083-1110`; POST `/api/podcast/generate`, poll GET `/api/podcast/status/[id]`; `lib/podcast-generator.ts`, podcast worker. Existing article ID; Gemini script/critic, OpenAI TTS, MP3 merge, optional Drive backup, Spaces/DB. No RSS publishing, mastering, music, or multi-host distribution (`GENERATION_PIPELINES.md:53-54`).
- SEO tools: UI `app/seo-tools/page.tsx:92-258`; POST `/api/seo/content-audit`, `/local-research`, `/competitor-analysis`, `/schema-markup`, `/content-structure`, `/pillar-cluster`, `/create-articles`; inputs are URL/content/topic/keywords/competitor/local location/schema/article params (exact schemas in each route). Provider-backed analysis/generation uses SEO services + Gemini/OpenAI; create-articles is the executable article-producing path, other endpoints are analyses/plans/markup. DB/credits/provider keys required.
- Briefs: home/dashboard brief generation `app/home/page.tsx:54`; POST `/api/briefs/generate-me`; admin UI `app/admin/briefs/page.tsx:149`; POST `/api/briefs/generate-now`, admin GET/management `/api/admin/briefs`; GET `/api/briefs/today`, PATCH `/api/briefs/preferences`, POST `/api/briefs/[id]/viewed`. Daily brief BullMQ/AI-research worker. Minimal authenticated user/team + preferences/date; DB, queue, research/provider dependencies.
- Campaign planning/ads: `/campaigns/new`, `/campaigns/[id]`, `app/campaigns/ads-lab.tsx`; POST/GET `/api/campaigns/[id]/ads`, POST `/api/campaigns/[id]/confirm-brand`; Gemini JSON via `lib/campaign-ads-service.ts`. Inputs campaign, objective/audience/budget/landingPageUrl/brand; landing URL must already exist. Google RSA + Meta validation; export/approve POST `/api/campaigns/[id]/ads/[adId]/approve`, GET export. Export-only: no Google/Meta API publishing/spend (`GENERATION_PIPELINES.md:33-35,44-50`).
- Journeys: UI `app/journeys/page.tsx`; POST `/api/journeys`, `/api/journeys/[id]/trigger`, `/api/batches/[id]/launch-journey`; GET/POST recommendation `/api/journey/next`, `/api/journey/convert`; PATCH steps `/api/journeys/[id]/steps/[stepId]`; templates/stats routes. `lib/journey-orchestrator-service.ts`, scheduler/queue. Journey scheduling/context/conversion executable, but no verified step->asset-generator enqueue and no demonstrated email asset generation (`GENERATION_PIPELINES.md:36,46-49`).
- Agency reports: UI `app/agency/reports/page.tsx:118`; POST `/api/agency/reports/generate`, approve/send/download routes. Deterministic DB evidence/rendering, not AI content generation (`GENERATION_PIPELINES.md:38`).

BRAND INTELLIGENCE / LEARNING / ANALYSIS
- Brand intelligence services: `lib/client-brand-profile-service.ts`, `lib/branding.ts`, `lib/competitive-intelligence-service.ts`, `lib/learning-integration.ts`; campaign confirm-brand and article/social/video generators consume context (video script explicitly has brand context at `lib/gemini-video-script-generator.ts:258-277`). No standalone customer “generate brand” button found; brand is prerequisite/context and research orchestration.
- Learning/decisioning UI `app/learning/page.tsx:260`; endpoints POST `/api/learning/feedback`, `/engagement`, `/mine-corpus`, `/monitor/mine-corpus`; GET `/intelligence`, `/strategy`, `/content-types`, `/patterns/[contentType]`, `/optimization/[contentType]`, monitor snapshot; agents seed/optimize/patterns routes also exist. Services `lib/learning-service.ts`, `lib/cohort-strategy-service.ts`, `lib/bayesian-decision-service.ts`, `lib/learning-monitor-service.ts`. Inputs are tenant/content IDs, feedback/engagement events, corpus/date/content type; DB is essential, model calls only in some analyses. Mostly learning/analysis, not guaranteed asset generation.
- Admin AI analysis: `/api/admin/incidents/[id]/analysis` (incident intelligence; `lib/incident-intelligence/ai-analysis.ts`), `/api/admin/seo-report`, `/api/admin/briefs` and analytics routes. Admin auth/incident or report inputs + DB/provider config required. Admin content library/articles are management, not separate generator.

PUBLISHING / BACKGROUND
- Background queues/workers: `lib/pipeline-worker.ts` (article/image), `lib/social-worker.ts` (social), `lib/podcast-worker.ts`, `lib/worker.ts`, `lib/queue.ts`, `lib/journey-orchestrator-service.ts`, scheduled brief worker, publishing worker. Publishing UI/API `/api/publishing/jobs` (referenced by matrix) has adapters only for website/Facebook/LinkedIn/TikTok; no X/Instagram/Pinterest/YouTube adapter. Queue acceptance is not provider/output E2E.

UNSUPPORTED / CODE-ONLY DISTINCTIONS
- No landing-page generator; existing HTTPS URL only. No AI email-campaign generator/marketing automation. No live ad publishing/spend. No image editing/inpainting. No one-click atomic article→social→video→podcast→email→ads flywheel. Threads/YouTube generation contracts absent. Veo cancellation cannot revoke submitted provider operation. All “implemented/live unverified” statuses are documented in `GENERATION_PIPELINES.md:19-40`; retrieval acceptance requires authenticated durable media proxy and valid playable file (`:64-80`).