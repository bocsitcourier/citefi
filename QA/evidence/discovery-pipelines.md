Discovery completed read-only; workspace policy prohibits creating/modifying `qa/evidence/discovery-pipelines.md`, so no file was written. Current source baseline: `reports/live-generation/masterinventory.json:3-684` contains 40 rows; rows 35-40 (Landing-page generation, AI email-campaign generation, live Google/Meta ad publishing/spend, provider-backed image editing/inpainting, atomic article-to-all-channels flywheel, Threads/YouTube generation contracts) are source-proven unsupported/NOT_APPLICABLE and must be annotated, not tested. Row 40 is Standalone Brand Intelligence and remains in scope.

Primary source map/evidence: `GENERATION_PIPELINES.md:17-40` maps UI→API→worker/service→provider→storage/retrieval for title pool, batch/article, metadata, formatting/hyperlinks, images, social, video, podcast, briefs, campaigns/ads, exports, journeys, publishing, reports, learning. `GENERATION_PIPELINES.md:42-62` explicitly documents the six unsupported surfaces and Veo cancellation boundary. `GENERATION_PIPELINES.md:64-88` defines retrieval acceptance and identifies replay/cap/lease/provider-operation gaps. `AI_ARCHITECTURE.md:26-41` gives canonical route→Postgres reservation→BullMQ/Redis→worker→provider/local media→validation→Postgres/Spaces→authenticated output topology; `:112-118` gives billing order; `:141-178` batch replay/reconciliation defects; `:212-230` known architectural gaps; `:245-291` direct-image and podcast settlement/cap defects. `replit.md:18-25,68-83` is project memory/governance and queue architecture.

Existing safe offline candidates after source inspection (tests use mocks/faults/simulated providers; do not label live verified): `tests/pipeline/pipeline-worker.test.ts`, `tests/pipeline/restart-safety.test.ts`, `tests/pipeline/restart-crash-boundaries.test.ts`, `tests/pipeline/execution-contract.test.ts`, `tests/pipeline/budget-stop-cleanup.test.ts`, `tests/pipeline/provider-circuit-breaker.test.ts`, `tests/media-provider-replay-safety.test.ts`, `tests/media-provider-receipt-boundary.test.ts`, `tests/generation-finalization-gate.test.ts`, `tests/article-output-safety.test.ts`, `tests/social-generation-contract.test.ts`, `tests/podcast-duration.test.ts`, `tests/batches/*.test.ts`, `tests/provider-*.test.ts`, `tests/brand-intelligence-validation.test.ts`, `tests/scope-0-*.test.ts`, `tests/campaigns/*.test.ts`, `tests/security/*.test.ts`, `tests/governance/launch-governance.test.ts`. No execution performed.

Confirmed source defects (not predictions): batch ambiguous replay can regress RUNNING/overwrite CANCELLED (`AI_ARCHITECTURE.md:141-145`); direct-image different-version concurrent preflights can double-submit and post-commit response loss permits paid duplicate (`AI_ARCHITECTURE.md:245-263`); podcast post-debit checkpoint failure can delete delivered media/mark failed/permit paid retry and sweeper uses obsolete ID (`AI_ARCHITECTURE.md:279-285`); podcast pending cap reservation is not passed to worker and direct-image completed-usage failure is swallowed (`:287-291`); Veo operation identity is memory-only before polling and cancellation cannot abort submitted work (`:214-224,263-268`); journeys lack repository evidence of direct asset-generator enqueue (`GENERATION_PIPELINES.md:36`); publisher adapters absent for X/Instagram/Pinterest/YouTube (`:37,58-60`).

Failure/retry/cancel/partial/gating focus: shared retry/accounting policy is `lib/pipeline-worker.ts`; social cancellation/partial READY behavior is `lib/social-video-generator.ts:316-327`; video worker classification/retry is `workers/video-idea-worker.ts:297-365`; malformed provider JSON parse sites include `lib/social-video-generator.ts:152-158`, `lib/seo-regenerator.ts:172,212,251`, `lib/fact-validated-generators.ts:329`, `lib/contextual-anchor-generator.ts:97`; provider receipt/accounting boundaries are covered by `lib/provider-attempt-receipts.ts`, `lib/provider-usage-ledger.ts`, `lib/media-provider-boundary.ts` and tests above. Prompt-injection/malformed JSON requires fixture-only mocked inputs and validator assertions; no provider/network/DB-write execution was performed. Code/mock/integration/live distinction must remain explicit: `GENERATION_PIPELINES.md:5-9,64-80` and `AI_ARCHITECTURE.md:3-15,232-243` state static/simulated tests do not certify live providers, retrieval, playback, publication, or export. Tenfold/hundredfold risks are queue concurrency/replay, per-provider lease absence, cap/ledger undercount, and partial batch fan-out; these are architectural risk multipliers, not live findings.