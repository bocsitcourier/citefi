Discovery completed read-only; requested write target `reports/qa/evidence/discovery-application.md` is absent, and workspace policy prohibits creating/modifying it. Current-source evidence: `replit.md:18-25,30-41,56-65,90-96` defines review gate, Next/Drizzle/Postgres/Redis architecture, tenant/admin controls, generation/publishing/media/error systems; `reports/qa-remediation/QA_REMEDIATION_STATE.md:27-45,147-177,211-279` is authoritative memory: no paid/provider/network/DB writes, 40 historical features = 27 addressable + 7 retained baseline + 6 unsupported (`NOT_TESTABLE`), no feature `VERIFIED_PASS`; task-179 notification is not proof of landed/merged/reconciled (`:99-102,117-120,201-209`). Counts from source: 343 `app` files, 256 `app/api` files, 71 page routes, 97 test files, 88 files containing executable test declarations; inventory target `reports/live-generation/masterinventory.json` has 40 rows (historical statuses, not current certification). Route surfaces include auth/signup/login/2FA/password (`app/api/auth/*`), content/review/approve/update/regeneration, batches/jobs/generation, media/upload, publishing connections/jobs/callbacks, analytics/events, journeys/learning/decisioning, and 256 total API handlers; UI surfaces include signup/login, onboarding, dashboard/client/workspace, content/editor, batches, social/video/podcast, publishing/settings, analytics, and admin pages (`app/admin/*`). End-to-end map: signup/login → onboarding/team context (`app/api/auth/signup`, `app/api/auth/login`, `app/api/auth/team-context`, `app/onboarding`) → workspace/client/team → title/job/article generation (`app/api/jobs/*`, `app/api/content/*`, `app/api/articles/*`) → editing/review/approval (`app/api/content/[id]/update`, `/review`, `/approve`) → publishing (`app/api/publishing/*`, `app/api/publish/*`) → analytics/events (`app/api/events/*`, `app/api/admin/analytics*`); admin path is `app/admin/*` plus `app/api/admin/*` (user approval/roles/sessions/quotas/credits/errors/analytics). Authz source: `server/middleware/auth.ts:26-65` requires Bearer JWT, active session, expiry checks; `:67-109` verifies active user/team membership and selected team; `:117-130` establishes tenant context; `:141-168` role checks/admin/team-member guards. CSRF source: `proxy.ts:120-154` gates unsafe cookie-auth API mutations with origin + CSRF cookie/header + HMAC proof; bearer/capability paths are exempt (`:121-127`). Confirmed source-level security finding: `proxy.ts:161-170` bypasses admin page-navigation JWT enforcement whenever `NODE_ENV=development` and no token exists; API handler auth is stated as authoritative, so this is not by itself an API privilege escalation, but it is a reproducible source behavior requiring environment-scoped QA (GET `/admin` with no token in development reaches page). Other source observations requiring tests, not defects: proxy explicitly documents cross-site preview SameSite cookie limitation (`proxy.ts:15-22`); bearer extraction accepts only `Bearer ` (`:100-112`), while Express auth also requires Bearer (`server/middleware/auth.ts:28-39`). Upload/injection/secret surfaces require offline inspection next: `app/api/media/upload`, `app/api/upload/logo`, public objects, HTML rendering (`dangerouslySetInnerHTML` matches exist); no claim made without route-specific import/setup inspection. Existing safe offline candidates identified (must remain mocked/deterministic and not executed here): `tests/auth/auth-api.test.ts`, `tests/auth/csrf.test.ts`, `tests/auth/password-recovery-security.test.ts`, `tests/auth/preview-session-fallback.test.ts`, `tests/security/client-csrf-fetch-contract.test.ts`, `tests/security/tenant-rls.test.ts`, `tests/admin/*`, `tests/batches/regenerate-titles-auth.test.ts`, `tests/generation-finalization-gate.test.ts`, `tests/article-output-safety.test.ts`, plus provider receipt/accounting contract tests. `package.json:6-18` confirms test scripts include DB/migration/provider-capable commands; do not run absent authorization. Code/mock/integration/live distinction: source inspection and existing test files are evidence only; prior inventory PASS and regression results remain `FIXED_UNVERIFIED` guardrails, not live feature certification. Risk framing: 10x/100x scale risks are predictions only (CSRF/origin configuration drift, tenant-ID/ownership gaps across 256 handlers, upload/object exposure, queue/provider accounting concurrency); no predicted risk labeled defect. No providers, network, DB writes, restarts, secrets, scans, or tests were executed.