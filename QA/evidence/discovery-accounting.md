PHASE1 discovery (workspace read-only; I did not modify/write QA/evidence/discovery-accounting.md or execute tests/providers/network/DB writes).

Relevant memory/contracts inspected: `AGENTS.md:1-9`; `docs/provider-attempt-receipts-contract.md:1-145`; `reports/qa-remediation/QA_REMEDIATION_PLAN.md`; `reports/live-generation/masterinventory.json:1-164+` (historical 40-feature baseline; statuses remain historical and must not be upgraded; unsupported six remain NOT_APPLICABLE). Task #179 receipt contract is current source.

Current merged receipt surfaces:
- Core: `lib/provider-attempt-receipts.ts:1-80,87-188,316-404,406-466,514-605,607-705,740-782` (allowlisted metadata; tenant ownership before admission; CAS submitted gate; known usage required; deterministic sourceEventId; strict spool validation). Contract explicitly says capture independent of ledger and spool reconciliation never calls providers/invents usage: `docs/provider-attempt-receipts-contract.md:54-85`.
- Recovery/status/idempotency/spool requirements: `docs/provider-attempt-receipts-contract.md:87-137`; historical limitation `:139-145`.
- Adapter receipt wrappers discovered: `lib/gemini-attempt-receipt.ts`, `lib/brave-attempt-receipt.ts`, `lib/openai-client.ts`, `lib/media-provider-boundary.ts`, plus direct/indirect provider callers in `lib/gemini.ts`, `lib/gemini-image-generator.ts`, `lib/gemini-social-image-generator.ts`, `lib/gemini-social.ts`, `lib/gemini-video-script-generator.ts`, `lib/veo-*.ts`, `lib/openai-*.ts`, `lib/audio-director.ts`, `lib/podcast-generator.ts`, `lib/competitive-intelligence-service.ts`, `lib/smart-topic-research.ts`, `lib/seo-intelligence.ts`, `lib/social-video-image-generator.ts`, and route `app/api/batches/[id]/regenerate-images/route.ts`. Static boundary coverage enumerates direct sinks (Gemini content/videos, OpenAI chat/responses/images/TTS, Brave fetch) in `tests/provider-accounting-boundary.test.ts:24-41`, transitive callers at `:99-127`.
- Accounting/caps/error policy/storage/migration surfaces: `lib/cost-telemetry.ts`, `lib/provider-usage-ledger.ts`, `lib/provider-attempt-object-spool.ts`, `lib/provider-invocation-identity.ts`, `migrations/0034_provider_attempt_receipts.sql`, `migrations/0034_provider_attempt_receipts_rollback.sql`, `scripts/reconcile-provider-attempt.ts`, `tests/provider-accounting-boundary.test.ts`, `tests/provider-receipt-error-policy.test.ts`, `tests/provider-attempt-object-spool.test.ts`, `tests/provider-attempt-migration.test.sh`, `tests/provider-usage-ledger*.test.ts`, `tests/batches/cap-reservation-contract.test.ts`, `tests/billing/*`.

Safe offline candidate tests after import/setup inspection (not run):
- Pure/in-memory receipt behavior: `tests/provider-attempt-receipts.test.ts:1-80` imports core only, sets dummy DATABASE_URL, uses MemoryStore/MemorySpool; inspect remaining cases for concurrency, missing usage, accounting failure, replay/ownership.
- Provider-specific mocked boundaries: `tests/openai-attempt-receipt.test.ts`, `tests/gemini-attempt-receipt.test.ts`, `tests/brave-attempt-receipt.test.ts`, `tests/media-provider-receipt-boundary.test.ts`, `tests/media-provider-replay-safety.test.ts`; each injects memory deps/stub submitters rather than SDK/network (confirm per test before execution).
- Static/source-only: `tests/provider-accounting-boundary.test.ts:148-270` scans source and has no provider call; `tests/provider-invocation-identity.test.ts`; migration shell test is DB-writing and NOT safe in this phase. `tests/provider-http-invocation.test.ts` may exercise HTTP and is not safe without import inspection/explicit disabling.
- Existing command conventions: package scripts use `node --import tsx/esm --test`; integration scripts may apply migrations/DB writes (`package.json:12-17`), so do not run. No global external-call-disable switch was established from inspected package scripts; candidates require injected mocks/stubs or source-only mode. `tests/scope-0-*.test.ts` use Node experimental module mocks but are feature-specific.

Confirmed defects vs predictions: No new defect may be confirmed without execution (none executed). Source/test assertions encode current intended invariants, not verification. Static test itself asserts no direct accounting-boundary violations (`tests/provider-accounting-boundary.test.ts:148-170`) and no swallowed accounting failures (`:206-270`), but this is source coverage only. Prediction/risk candidates: uninspected adapter paths may bypass receipt wrapper; direct sink regex coverage can miss indirect SDK aliases/dynamic imports; receipt DB prepare/CAS and spool readiness are hard to validate without DB/filesystem integration. 10x/100x risks: concurrent workers/adapters sharing one invocation identity can amplify duplicate paid submissions; non-shared/ephemeral spool can block admission or strand reconciliation; caps/credit settlement race under burst fanout can multiply reservations; partial provider usage or accounting outage can create reconciliation backlog. These are risks, not defects.

Historical forensic chain (safe artifact only): `reports/live-generation/assets/podcast-2238-transcription-v1-forensics.json:1-65` records team/article placeholders (team 2535/article 2238), zero calls during forensics, target status PROVIDER_ACCOUNTING_FAILED, providerRequestId null, ledgerRows 0, physical outcome unreconciled/unknown, and failure due missing tenant DB context (`:12-18`). Original harness/model/request config/output cap/input limits/usage are unknown (`:20-33`). Later v2 model/limit/request ID/ledger ID are explicitly separate non-evidence (`:35-50`). Artifact gives stress bounds $2.78528 and conditional $5.57056 but says model limits unconfirmed and no expense estimate/fake ledger event (`:52-58`); do not invent cost or call it confirmed expense.

Strongest tests to create after Phase1 (offline first, then authorized integration): (1) injected DB-failure-after-provider-response proves receipt/spool captures usage, marks accounting_failed, and reconciliation is idempotent with no provider retry; (2) concurrent same invocation across 10x/100x workers proves exactly one submit/CAS; (3) every adapter matrix (text/image/audio/video/search/aux LLM) asserts safe metadata excludes prompts/bytes/credentials and captures provider-native usage; (4) missing/partial usage is terminal/non-zero-inventing; (5) shared spool readiness/permissions and malformed-record rejection; (6) cap/credit reservation races and error-policy propagation through transitive callers; (7) migration/RLS/storage ownership and client non-exposure. Maintain code-vs-mock-vs-integration-vs-live labels; current candidates are code/static or mocked, not VERIFIED/live.