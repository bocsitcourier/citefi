Discovery-only findings (workspace read-only; could not write `qa/evidence/discovery-operations.md`):

Memory reviewed: `.agents/memory/MEMORY.md` (Neon transaction/target safety, BullMQ/Redis, deployment, billing/COGS, storage, RLS, reservation, generation-audit guidance). No feature is source-VERIFIED.

Receipt/migration surfaces:
- `migrations/0034_provider_attempt_receipts.sql:1-106`: durable receipt schema/statuses, team/campaign composite FK, indexes, grants, ENABLE+FORCE RLS, client-viewer exclusion. Rollback is `migrations/0034_provider_attempt_receipts_rollback.sql:1-12`.
- `scripts/run-versioned-migrations.ts:9-32,50-69`: runner has two files beginning `0034_` (`0034_agency_report_period_unique.sql`, then `0034_provider_attempt_receipts.sql`); ledger key is full filename, so they are distinct and deterministic in current array order. Real ordering risk: `MIGRATION_START_VERSION` uses lexical filename filtering (`:50-56`), so starting at `0034_provider...` skips `0034_agency...`; starting at generic `0034` includes both. There is no rollback orchestration. Runner requires `DATABASE_URL`/`NEON_DATABASE_URL` (`:33-39`) and performs real DB writes; not a safe discovery test target. Its final catalog verification always runs post-schema checks, while incident verification is skipped for start versions >= `0022` (`:71-171`), making partial-start certification incomplete.
- `lib/provider-attempt-receipts.ts:1-10,514-605`: DB store uses `getTxDb()` transactions; prepare uses source-event conflict idempotency and metadata binding; `markSubmitted` CAS refuses duplicate physical submission (`:557-570`). Response capture/status updates are separate writes (`:572-597`), requiring spool fallback/reconciliation review.
- Receipt spool parser is strict/allow-listed (`lib/provider-attempt-receipts.ts:607-782+`); no prompt/content/credential fields intended. Safe test candidates are memory-store tests only.

Existing safe offline candidates (inspect-only; do not execute):
- `tests/provider-attempt-receipts.test.ts:35-90` ownership-before-submit and concurrent single-admission; `:92-159` DB/CAS/spool outage fail-closed; `:161-215` response-save outage and retry dedupe; `:217-264` partial usage never billed; `:266-336` sanitized provider failures; `:338-428` ledger outage/committed-then-throw reconciliation; `:467-499` tenant ownership on reconciliation; `:501-551` deterministic identity/async usage; `:553-568` strict spool parsing. Imports are local modules and use `MemoryProviderAttemptReceiptStore/Spool`; no provider/network calls.
- `tests/provider-attempt-migration.test.sh:1-14,29-66` is isolated local PostgreSQL, but creates/tears down a temporary DB and runs migration writes; candidate only under explicit controlled QA authorization, not read-only discovery.
- `tests/provider-receipt-error-policy.test.ts`, `tests/*attempt-receipt*.test.ts`, `tests/gemini-attempt-receipt.test.ts`, `tests/openai-attempt-receipt.test.ts`, `tests/brave-attempt-receipt.test.ts`, `tests/media-provider-receipt-boundary.test.ts` are further likely mock/unit candidates; inspect setup before execution. Integration files such as `tests/provider-usage-ledger.integration.test.ts` use database fixtures/writes and are not offline.

Confirmed source concerns vs forecasts:
- Confirmed: versioned runner’s filename-based `MIGRATION_START_VERSION` can skip the other duplicate-0034 migration; partial-start verification can omit incident controls. These are source-confirmed ordering/certification defects, not predictions.
- Confirmed: receipt status/capture/accounting persistence is multi-step; crash windows require spool/reconciliation evidence. This is an architectural test surface, not proof of runtime failure.
- Forecast only (not load-tested): 10x/100x risks include receipt/spool growth, per-receipt DB/index write amplification, BullMQ retry storms, concurrent publishing/webhook claims, email delivery contention, and rollback/restart recovery. No performance/load certification exists from source.

Other requested surfaces found by search but not certified: publishing routes under `app/api/publishing/**`, adapters under `lib/publishing/**`, billing/webhook routes under `app/api/billing/**` and `app/api/stripe/**`, `lib/email.ts`, Redis helper `tests/helpers/isolated-redis.ts`, storage migration `lib/storage-migration.ts`/`tests/storage-migration.test.ts`. Distinguish code/mock/integration/live explicitly; no live provider, network, DB, restart, deployment, email, payment, or queue execution was performed.