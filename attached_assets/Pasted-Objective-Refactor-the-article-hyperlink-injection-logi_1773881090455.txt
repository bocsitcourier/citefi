Objective: Refactor the article hyperlink injection logic to fix a zero-match bug and an HTML unformatting bug.

Context & Root Cause:
Currently, hyperlinks are failing because anchor texts are selected before the final GPT-4 rewrite. The final text changes, resulting in 0 matches. Furthermore, doing a raw String.replace() on the entire HTML string is destroying HTML attributes (like class or src) and breaking the page format.

Implementation Requirements:
Rewrite the hyperlink injection function using cheerio. The function must take the final GPT-4 generated HTML string and a slugMap (dictionary of topics to URLs).

Follow this exact logic:

Parse Safely: Load the final HTML using cheerio.load(html, null, false).

Target Safe Elements: Iterate only over text-heavy elements: $('p, li, td'). Do not touch <h>, <img>, or <script> tags.

Prevent Nesting: Inside the loop, check if the current element is already inside an <a> tag or contains an <a> tag. If it does, skip it to prevent nested links.

Regex Word Boundaries: For each key in the slugMap, create a case-insensitive regular expression with word boundaries: new RegExp('\\b(' + escapeRegExp(keyword) + ')\\b', 'i').

Safe Replacement: Check if the element's .html() contains the regex. If it does, replace the matched text with <a href="${url}">${matchedText}</a>. Apply a maximum of 1 link per keyword per article to avoid SEO spam.

Return: Return the updated $.html().

Execution:
Update the code now. Make sure cheerio is in the package.json. Add a helper function to escape regex characters so facility names with special characters don't crash the script.

Why this prompt guarantees a fix:
cheerio.load(html, null, false): The false parameter is critical. Without it, Cheerio will automatically wrap your article in <html><head><body> tags, which ruins your layout if you are injecting this article into a larger template later.

$('p, li, td'): By explicitly telling the Agent to only search inside paragraphs and lists, it physically cannot accidentally overwrite an <img> tag or a class="container" attribute.

Max 1 Link per Keyword: I added this rule to the prompt because Google’s algorithm penalizes "over-optimized" internal linking. If the word "nursing home" appears 40 times in a 2000-word article, you only want the first one linked.