The "Safe Boundary" Regex LogicInstead of \b, which treats a dot as a boundary and breaks the match, we use Lookaheads and Lookbehinds. This ensures we only match the keyword if it is surrounded by whitespace or punctuation, but not if it is part of another word.Update the applySmartHyperlinks helper with this refined regex:TypeScriptfunction escapeRegExp(string: string) {
  return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

// THE PERMANENT FIX FOR SPECIAL CHARACTERS
const escapedKeyword = escapeRegExp(keyword);

/**
 * Breakdown of the Regex:
 * (?<=^|[^a-zA-Z0-9])  -> Lookbehind: Match start of string OR any non-alphanumeric char
 * (${escapedKeyword})  -> The actual facility name (e.g., "St. John's")
 * (?=$|[^a-zA-Z0-9])   -> Lookahead: Match end of string OR any non-alphanumeric char
 */
const regex = new RegExp(`(?<=^|[^a-zA-Z0-9])${escapedKeyword}(?=$|[^a-zA-Z0-9])`, 'i');
The "Final Architecture" ChecklistBy implementing the Cheerio-based traversal with this advanced Regex, your pipeline now solves all previous "Gaps":RiskThe "Cheerio + Lookahead" SolutionPartial MatchesIf a facility is "Care Center", it won't accidentally link the word "Care" inside "Healthcare" because of the alphanumeric lookbehind.Special CharactersNames like "St. Joseph's" or "U-Mass" will now match perfectly because we aren't relying on the flawed \b boundary.Nested LinksThe Cheerio traversal if (node.parent.name === 'a') return; ensures you never get <a><a>text</a></a>, which crashes SEO.Attribute CorruptionSince we only touch node.type === 'text', your <img src="..."> and <div class="..."> are physically untouchable by the script.Summary of the Clean-Up Task for Replit AgentDelete the "Placeholder" system (the ___HEADER_PROTECT___ logic). It is technical debt that causes the "unformatting" bug.Consolidate all hyperlinking into one lib/hyperlink-engine.ts file using the Cheerio logic above.Update the worker.ts to call this new engine after the GPT-4 rewrite is finished.Logging: Add a line: console.log(\[Hyperlink] Injected ${linksAdded} links for ${facilityName}`);` so you can monitor success in the Replit console.