I. Architectural Fix: Enforcing Data Fidelity via TokenizationThe solution is to transform the URL into a secure, non-generative token that is only merged into the final script after the Gemini model has completed its generation task.Tokenization Strategy: The lib/services/video-worker.ts should replace the original URL with a distinct, recognizable token (e.g., ]) before sending the prompt to lib/ai/gemini.ts.Prompt Constraint: The prompt template (lib/ai/prompts/video-generation.ts) must be strictly constrained to use only this token for the final Call-to-Action (CTA).Post-Generation Merge: The video-worker.ts waits for the Gemini response, validates the presence of the token, and then surgically replaces the token with the original, verified URL.II. CAD Framework Prompt: Auditing and Refactoring the CodeUse the following constrained, multi-turn prompt sequence with your Replit AI agent. This sequence forces the agent to audit the relevant files for security and data handling issues and propose a solution that implements the tokenization strategy.Prompt Template: Auditing Gemini Worker for URL HallucinationStepInstruction to AI (Role & Constraint)Contextual InputDebugging GoalStep 1: Role Assignment & Problem DefinitionRole: "Assume the role of a Senior Data Integrity Architect specializing in secure Gemini API integration and hallucination mitigation." Task: "Analyze the provided code from lib/services/video-worker.ts and the prompt template. The system is experiencing URL hallucination (Gemini is returning an incorrect URL) because the URL is passed as part of the creative prompt. This must be eliminated."1. The relevant function from lib/services/video-worker.ts that executes the prompt. 2. The code in lib/ai/gemini.ts that sends the request. 3. The current lib/ai/prompts/video-generation.ts template.Force the AI to diagnose the root cause: treating static data as generative text.Step 2: Refactoring for Tokenization (Surgical Fix)Goal: "Propose a refactor for the video-worker.ts function. This refactor must implement a tokenization strategy where the original URL is replaced by the non-generative token ] before the prompt is sent to Gemini." Constraint: "The original, verified URL must be merged back into the script only after the Gemini response is returned. The token replacement and merge logic should be explicit and robust." Type 'NEXT' to proceed.1. The code block currently responsible for preparing the script and the CTA.Ensures the Gemini LLM only generates creative text, eliminating the chance of hallucination by hiding the static data from the generation process.Step 3: Prompt Template & VerificationGoal: "Update the lib/ai/prompts/video-generation.ts template to enforce the token usage and add a final integrity check." Constraint: "The updated prompt must instruct Gemini to use the exact token ] for the final URL CTA. It must also mandate that Gemini output a machine-readable JSON verification block at the end of the script confirming the status, like this: {'verification_token_present': true}."