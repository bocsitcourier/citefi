import { db } from "./db";
import { eq, and, desc, sql, isNull } from "drizzle-orm";
import {
  learningPatterns,
  contentPerformanceMetrics,
  aiLearningLedger,
  articles,
  // NEW tables you'll need migrations for (see notes at bottom):
  contentReviews,     // one row per reviewed piece
  patternDimensionStats, // (patternId, dimension, successes, trials, wilson)
  ContentType,
} from "../shared/schema";
import { analyzeContentQuality } from "./deterministic-humanizer";
import { factStore } from "./fact-store";
import { GEMINI_ARTICLE_MODEL, GPT_ENHANCEMENT_MODEL } from "./ai-config";

// ============================================================================
// STATISTICS: Wilson lower bound — the honest success rate
// Replaces the broken `timesUsed / MIN_SAMPLES` confidence. A pattern that
// failed 5/5 times scores ~0 here; one that won 5/5 scores high but not 100
// (small samples stay humble). Self-corrects as data accumulates.
// ============================================================================
export function wilsonLowerBound(successes: number, trials: number, z = 1.96): number {
  if (trials === 0) return 0;
  const p = successes / trials;
  const denom = 1 + (z * z) / trials;
  const center = p + (z * z) / (2 * trials);
  const margin = z * Math.sqrt((p * (1 - p) + (z * z) / (4 * trials)) / trials);
  return Math.max(0, Math.round(((center - margin) / denom) * 100));
}

// ============================================================================
// DEFECT TAXONOMY — you can only learn from categorized failures
// ============================================================================
export const DEFECT = {
  // Completeness
  TRUNCATED:        { code: "completeness:truncated",        dim: "completeness", sev: "high" },
  MISSING_SECTION:  { code: "completeness:missing_section",  dim: "completeness", sev: "high" },
  THIN_SECTION:     { code: "completeness:thin_section",     dim: "completeness", sev: "medium" },
  UNANSWERED_BRIEF: { code: "completeness:unanswered_brief", dim: "completeness", sev: "high" },
  // Factuality / hallucination
  UNSUPPORTED_CLAIM:{ code: "factuality:unsupported_claim",  dim: "factuality",   sev: "high" },
  FAKE_CITATION:    { code: "factuality:fake_citation",      dim: "factuality",   sev: "high" },
  // Structure / AEO
  NO_ANSWER_FIRST:  { code: "structure:no_answer_first",     dim: "structure",    sev: "medium" },
  BAD_HEADINGS:     { code: "structure:bad_headings",        dim: "structure",    sev: "low" },
  MISSING_FAQ:      { code: "structure:missing_faq",         dim: "structure",    sev: "medium" },
  MISSING_SCHEMA:   { code: "structure:missing_schema",      dim: "structure",    sev: "low" },
  // Humanness
  AI_ISMS:          { code: "humanness:ai_isms",             dim: "humanness",    sev: "medium" },
  LOW_BURSTINESS:   { code: "humanness:low_burstiness",      dim: "humanness",    sev: "medium" },
  REPETITIVE_OPEN:  { code: "humanness:repetitive_openings", dim: "humanness",    sev: "low" },
  // Channel-specific (video / podcast)
  WEAK_HOOK:        { code: "channel:weak_hook",             dim: "engagement",   sev: "high" },
  NO_CTA:           { code: "channel:no_cta",                dim: "engagement",   sev: "medium" },
  NO_PACING:        { code: "channel:no_pacing_markers",     dim: "engagement",   sev: "low" },
} as const;

export type Dimension = "completeness" | "factuality" | "structure" | "humanness" | "engagement";
const ALL_DIMS: Dimension[] = ["completeness", "factuality", "structure", "humanness", "engagement"];
const PASS_THRESHOLD = 70; // dimension score >= this AND no high-sev defect in that dim => "success"

interface Defect { code: string; dim: Dimension; severity: string; evidence: string }
interface ReviewResult {
  contentId: number;
  contentType: string;
  dimensionScores: Record<Dimension, number>;
  defects: Defect[];
  passed: boolean;
}

interface Brief {
  targetWords?: number;
  keyword?: string;
  questions?: string[]; // the Reddit/brief questions this piece was supposed to answer
}

export class ContentReviewService {
  private static instance: ContentReviewService;
  static getInstance() {
    if (!this.instance) this.instance = new ContentReviewService();
    return this.instance;
  }

  // ==========================================================================
  // MAIN ENTRY: review one piece of content (3 tiers, cheap -> expensive)
  // ==========================================================================
  async reviewContent(
    teamId: number,
    contentId: number,
    contentType: string,
    content: string,
    brief: Brief = {},
    opts: { useJudge?: boolean } = {}
  ): Promise<ReviewResult> {
    const defects: Defect[] = [];

    // TIER 1 — deterministic, runs on EVERYTHING (free, reliable)
    defects.push(...this.deterministicChecks(content, contentType, brief));

    // TIER 2 — hallucination check against your fact-store (cheap-ish, semi-reliable)
    defects.push(...(await this.factualityChecks(teamId, content)));

    // TIER 3 — LLM judge (expensive) — only on a sample, or pieces that already
    // failed Tier 1/2, or high-value content. Judged with a DIFFERENT model
    // than the generator to avoid shared blind spots (see judge()).
    let judgeScores: Partial<Record<Dimension, number>> = {};
    if (opts.useJudge) {
      const j = await this.judge(content, contentType);
      judgeScores = j.scores;
      defects.push(...j.defects);
    }

    // Roll defects + judge into per-dimension scores
    const dimensionScores = this.scoreDimensions(defects, judgeScores, content);
    const hasHighDefect = (d: Dimension) =>
      defects.some(x => x.dim === d && x.severity === "high");
    const passed = ALL_DIMS.every(d => dimensionScores[d] >= PASS_THRESHOLD && !hasHighDefect(d));

    const result: ReviewResult = { contentId, contentType, dimensionScores, defects, passed };

    await db.insert(contentReviews).values({
      teamId, contentType,
      articleId: contentType === ContentType.ARTICLE ? contentId : null,
      dimensionScoresJson: dimensionScores,
      defectsJson: defects,
      passed: passed ? 1 : 0,
      reviewedAt: new Date(),
    });

    return result;
  }

  // ==========================================================================
  // TIER 1: deterministic checks — no model, no cost, fully reliable
  // ==========================================================================
  private deterministicChecks(content: string, contentType: string, brief: Brief): Defect[] {
    const defects: Defect[] = [];
    const text = content.replace(/<[^>]*>/g, " ").replace(/\s+/g, " ").trim();
    const words = text ? text.split(/\s+/).length : 0;

    // --- Completeness ---
    // Truncated: ends mid-sentence, or way under target length
    if (!/[.!?]["')\]]?\s*$/.test(text)) {
      defects.push({ ...DEFECT.TRUNCATED, evidence: "no terminal punctuation — likely cut off" });
    }
    if (brief.targetWords && words < brief.targetWords * 0.6) {
      defects.push({ ...DEFECT.TRUNCATED, evidence: `${words}/${brief.targetWords} words` });
    }
    // Brief-question coverage: did it actually answer what it was asked?
    // (keyword-overlap heuristic; upgrade to embedding similarity for accuracy)
    for (const q of brief.questions || []) {
      const keyTerms = q.toLowerCase().match(/\b\w{4,}\b/g) || [];
      const hit = keyTerms.filter(t => text.toLowerCase().includes(t)).length;
      if (keyTerms.length && hit / keyTerms.length < 0.4) {
        defects.push({ ...DEFECT.UNANSWERED_BRIEF, evidence: `unanswered: "${q.slice(0, 80)}"` });
      }
    }

    if (contentType === ContentType.ARTICLE) {
      // --- Structure / AEO ---
      const h2s = (content.match(/<h2[\s>]/gi) || []).length;
      if (h2s === 0) defects.push({ ...DEFECT.BAD_HEADINGS, evidence: "no H2 headings" });
      // thin sections: H2s with very little text between them
      if (h2s > 0 && words / h2s < 80) {
        defects.push({ ...DEFECT.THIN_SECTION, evidence: `~${Math.round(words / h2s)} words/section` });
      }
      if (!/frequently asked|<h[23][^>]*>\s*faq/i.test(content) && !/\bFAQ\b/.test(content)) {
        defects.push({ ...DEFECT.MISSING_FAQ, evidence: "no FAQ section" });
      }
      if (!/application\/ld\+json|"@type"/i.test(content)) {
        defects.push({ ...DEFECT.MISSING_SCHEMA, evidence: "no JSON-LD schema" });
      }
      // Answer-first: first ~80 words should contain the target keyword
      if (brief.keyword) {
        const intro = text.slice(0, 500).toLowerCase();
        if (!intro.includes(brief.keyword.toLowerCase())) {
          defects.push({ ...DEFECT.NO_ANSWER_FIRST, evidence: "keyword absent from opening" });
        }
      }
    }

    if (contentType === ContentType.VIDEO || contentType === ContentType.PODCAST) {
      // Channel-specific structural signals
      if (!/\[?hook\]?|0:0\d|first \d+ seconds/i.test(content)) {
        defects.push({ ...DEFECT.WEAK_HOOK, evidence: "no identifiable hook/open" });
      }
      if (!/subscribe|follow|link in|call to action|cta/i.test(content)) {
        defects.push({ ...DEFECT.NO_CTA, evidence: "no CTA found" });
      }
      if (contentType === ContentType.VIDEO && !/visual:|b-?roll|on-?screen|\[\d+:\d+\]/i.test(content)) {
        defects.push({ ...DEFECT.NO_PACING, evidence: "no visual/timecode direction" });
      }
    }

    // --- Humanness (reuse your existing analyzer) ---
    const q = analyzeContentQuality(text);
    if (q.aiIsmCount > 3) defects.push({ ...DEFECT.AI_ISMS, evidence: `${q.aiIsmCount} AI-isms` });
    if (q.burstiness < 0.3) defects.push({ ...DEFECT.LOW_BURSTINESS, evidence: `burstiness ${q.burstiness}` });
    // Repetitive openings: many sentences starting the same way
    const openers = (text.match(/(^|[.!?]\s+)(\w+\s+\w+)/g) || []).map(s => s.trim().toLowerCase());
    const dupes = openers.length - new Set(openers).size;
    if (openers.length > 10 && dupes / openers.length > 0.3) {
      defects.push({ ...DEFECT.REPETITIVE_OPEN, evidence: `${dupes} repeated sentence openers` });
    }

    return defects;
  }

  // ==========================================================================
  // TIER 2: hallucination detection via fact-store
  // CAVEAT: only catches claims you have facts ABOUT. An unsupported claim is
  // flagged as "unverifiable", NOT proven false. Claims outside your fact
  // coverage are invisible to this check — do not treat absence as innocence.
  // ==========================================================================
  private async factualityChecks(teamId: number, content: string): Promise<Defect[]> {
    const defects: Defect[] = [];
    const text = content.replace(/<[^>]*>/g, " ");

    // Extract whole sentences containing a checkable claim
    const claimRegex =
      /[^.!?]*?(\d+(\.\d+)?\s?%|\d+\s?percent|according to|studies show|research (shows|indicates|finds)|data (shows|suggests)|survey|in (19|20)\d{2})[^.!?]*[.!?]/gi;
    const claims = [...new Set(text.match(claimRegex) || [])].slice(0, 25);

    for (const claim of claims) {
      const facts = await factStore.searchFacts({ teamId, query: claim.slice(0, 120), limit: 3 });
      // crude support check — upgrade to embedding similarity >= threshold
      const supported = facts.length > 0;
      if (!supported) {
        defects.push({ ...DEFECT.UNSUPPORTED_CLAIM, evidence: claim.trim().slice(0, 140) });
      }
    }

    // Fake-citation heuristic: cites a source/URL with no matching fact
    const citations = text.match(/\(([^)]*(?:\.com|\.org|\.gov|et al\.|\d{4})[^)]*)\)/gi) || [];
    for (const cite of citations.slice(0, 15)) {
      const facts = await factStore.searchFacts({ teamId, query: cite.slice(0, 100), limit: 2 });
      if (facts.length === 0) {
        defects.push({ ...DEFECT.FAKE_CITATION, evidence: cite.slice(0, 120) });
      }
    }

    return defects;
  }

  // ==========================================================================
  // TIER 3: LLM-as-judge — rubric-driven, structured output, CROSS-MODEL
  // Generate with Gemini -> judge with GPT (and vice versa) so the judge
  // doesn't share the generator's blind spots ("grading its own homework").
  // ==========================================================================
  private async judge(
    content: string,
    contentType: string
  ): Promise<{ scores: Partial<Record<Dimension, number>>; defects: Defect[] }> {
    const rubric = this.rubricFor(contentType);
    const prompt = `You are a strict editorial reviewer. Score the content 0-100 on each dimension and list concrete, evidence-backed defects. Be harsh; a 70 is "publishable", 90+ is "excellent".

DIMENSIONS: completeness, factuality, structure, humanness, engagement
RUBRIC:
${rubric}

Return ONLY JSON:
{"completeness":n,"factuality":n,"structure":n,"humanness":n,"engagement":n,
 "defects":[{"code":"<dimension>:<short_slug>","evidence":"<quote or specifics>"}]}

CONTENT:
${content.slice(0, 8000)}`;

    try {
      // Pseudocode — wire to your model layer. KEY: pick the model NOT used to
      // generate this piece. e.g. if generated by GEMINI_ARTICLE_MODEL, judge
      // with GPT_ENHANCEMENT_MODEL.
      const raw = await this.callJudgeModel(GPT_ENHANCEMENT_MODEL, prompt);
      const parsed = JSON.parse(raw.replace(/```json|```/g, "").trim());
      const scores: Partial<Record<Dimension, number>> = {};
      for (const d of ALL_DIMS) if (typeof parsed[d] === "number") scores[d] = parsed[d];
      const defects: Defect[] = (parsed.defects || []).map((x: any) => ({
        code: x.code,
        dim: (x.code?.split(":")[0] || "structure") as Dimension,
        severity: "medium",
        evidence: x.evidence || "",
      }));
      return { scores, defects };
    } catch (e) {
      console.warn("⚠️ Judge failed, falling back to deterministic-only:", e);
      return { scores: {}, defects: [] };
    }
  }

  private rubricFor(contentType: string): string {
    const common = "completeness: every promised section present and substantive. structure: logical flow, scannable. humanness: varied sentences, no AI-isms, sounds like a knowledgeable person.";
    const byType: Record<string, string> = {
      [ContentType.ARTICLE]: `${common} factuality: claims are specific and sourceable. engagement: strong answer-first opening, useful throughout.`,
      [ContentType.VIDEO]: `${common} engagement: hook lands in first 3s, clear pacing, CTA feels natural. factuality: claims verifiable.`,
      [ContentType.PODCAST]: `${common} engagement: cold-open hook, energy variation, conversational. factuality: claims verifiable.`,
      [ContentType.SOCIAL]: `${common} engagement: scroll-stopping hook, drives a reply/share. factuality: no fabricated stats.`,
    };
    return byType[contentType] || byType[ContentType.ARTICLE];
  }

  private async callJudgeModel(_model: string, _prompt: string): Promise<string> {
    // TODO: connect to your existing model invocation helper.
    throw new Error("wire callJudgeModel to your model layer");
  }

  // ==========================================================================
  // Convert defects + judge scores into 0-100 per dimension
  // ==========================================================================
  private scoreDimensions(
    defects: Defect[],
    judge: Partial<Record<Dimension, number>>,
    content: string
  ): Record<Dimension, number> {
    const out = {} as Record<Dimension, number>;
    const penalty = { high: 35, medium: 15, low: 5 } as Record<string, number>;
    for (const d of ALL_DIMS) {
      // Start from judge score if available, else 100, then subtract defect penalties
      let score = judge[d] ?? 100;
      for (const def of defects.filter(x => x.dim === d)) {
        score -= penalty[def.severity] ?? 10;
      }
      out[d] = Math.max(0, Math.min(100, Math.round(score)));
    }
    return out;
  }

  // ==========================================================================
  // CORPUS MINING: review everything already generated, aggregate, and learn
  // ==========================================================================
  async mineCorpus(
    teamId: number,
    contentType: string,
    opts: { limit?: number; judgeSampleRate?: number } = {}
  ): Promise<{ reviewed: number; topDefects: Array<{ code: string; count: number }> }> {
    const judgeRate = opts.judgeSampleRate ?? 0.2;
    const rows = await db
      .select()
      .from(articles)
      .where(eq(articles.teamId, teamId))
      .orderBy(desc(articles.createdAt))
      .limit(opts.limit ?? 500);

    const defectCounts = new Map<string, number>();
    let reviewed = 0;

    for (const row of rows) {
      // Pull the perf metric to recover which patterns this piece used
      const [perf] = await db
        .select()
        .from(contentPerformanceMetrics)
        .where(
          and(
            eq(contentPerformanceMetrics.teamId, teamId),
            eq(contentPerformanceMetrics.articleId, row.id)
          )
        )
        .limit(1);

      const brief: Brief = {
        targetWords: 2000,
        keyword: (row as any).keyword ?? undefined,
        questions: ((row.metadata as any)?.research?.redditQuestions || [])
          .slice(0, 10)
          .map((q: any) => q.title),
      };

      const review = await this.reviewContent(
        teamId, row.id, contentType, row.content, brief,
        { useJudge: Math.random() < judgeRate }
      );
      reviewed++;
      for (const d of review.defects) defectCounts.set(d.code, (defectCounts.get(d.code) || 0) + 1);

      // ATTRIBUTION: update each used pattern's per-dimension Wilson stats
      const patternsUsed = (perf?.patternsUsedJson as number[]) || [];
      await this.attributeReview(patternsUsed, review);

      // WIN MINING: defect-free, high-scoring pieces become exemplars
      if (review.passed && ALL_DIMS.every(d => review.dimensionScores[d] >= 85)) {
        await this.captureExemplar(teamId, contentType, row.id, row.content);
      }
    }

    // Turn the worst recurring defects into Guardian-ledger entries ->
    // these become NEGATIVE prompt constraints on the next generation.
    const topDefects = [...defectCounts.entries()]
      .sort((a, b) => b[1] - a[1])
      .slice(0, 8)
      .map(([code, count]) => ({ code, count }));

    await this.recordCorpusDefects(teamId, contentType, topDefects);

    console.log(`🔬 Mined ${reviewed} ${contentType} pieces. Top defect: ${topDefects[0]?.code} (${topDefects[0]?.count}x)`);
    return { reviewed, topDefects };
  }

  // ==========================================================================
  // ATTRIBUTION: a piece's per-dimension outcome credits/blames its patterns
  // ==========================================================================
  private async attributeReview(patternIds: number[], review: ReviewResult): Promise<void> {
    for (const dim of ALL_DIMS) {
      const hasHigh = review.defects.some(d => d.dim === dim && d.severity === "high");
      const success = review.dimensionScores[dim] >= PASS_THRESHOLD && !hasHigh;
      for (const pid of patternIds) {
        await this.updatePatternDimension(pid, dim, success);
      }
    }
  }

  private async updatePatternDimension(
    patternId: number,
    dimension: Dimension,
    success: boolean
  ): Promise<void> {
    const [stat] = await db
      .select()
      .from(patternDimensionStats)
      .where(
        and(
          eq(patternDimensionStats.patternId, patternId),
          eq(patternDimensionStats.dimension, dimension)
        )
      )
      .limit(1);

    const successes = (stat?.successes || 0) + (success ? 1 : 0);
    const trials = (stat?.trials || 0) + 1;
    const wilson = wilsonLowerBound(successes, trials);

    if (stat) {
      await db
        .update(patternDimensionStats)
        .set({ successes, trials, wilsonScore: wilson, updatedAt: new Date() })
        .where(eq(patternDimensionStats.id, stat.id));
    } else {
      await db.insert(patternDimensionStats).values({
        patternId, dimension, successes, trials, wilsonScore: wilson, updatedAt: new Date(),
      });
    }
  }

  // ==========================================================================
  // Feed recurring defects back as avoid-list (uses your aiLearningLedger)
  // ==========================================================================
  private async recordCorpusDefects(
    teamId: number,
    contentType: string,
    defects: Array<{ code: string; count: number }>
  ): Promise<void> {
    const now = new Date();
    for (const { code, count } of defects) {
      const errorType = code.toUpperCase();
      const [existing] = await db
        .select()
        .from(aiLearningLedger)
        .where(
          and(
            eq(aiLearningLedger.teamId, teamId),
            eq(aiLearningLedger.contentType, contentType),
            eq(aiLearningLedger.errorType, errorType)
          )
        )
        .limit(1);
      if (existing) {
        await db.update(aiLearningLedger)
          .set({ count: existing.count + count, lastOccurrence: now })
          .where(eq(aiLearningLedger.id, existing.id));
      } else {
        await db.insert(aiLearningLedger).values({ teamId, contentType, errorType, count, lastOccurrence: now });
      }
    }
  }

  // Map the top recurring defects into explicit "DON'T" lines for the prompt.
  // Call this from your LearningService.buildPromptEnhancements().
  async getNegativeConstraints(teamId: number, contentType: string, limit = 5): Promise<string[]> {
    const rows = await db
      .select()
      .from(aiLearningLedger)
      .where(and(eq(aiLearningLedger.teamId, teamId), eq(aiLearningLedger.contentType, contentType)))
      .orderBy(desc(aiLearningLedger.count))
      .limit(limit);

    const MAP: Record<string, string> = {
      "COMPLETENESS:TRUNCATED": "Finish every section. Never end mid-thought or cut the article short.",
      "COMPLETENESS:UNANSWERED_BRIEF": "Explicitly answer every question in the brief.",
      "COMPLETENESS:THIN_SECTION": "Each H2 section must have at least 120 words of substance.",
      "FACTUALITY:UNSUPPORTED_CLAIM": "Only state statistics/claims you can ground in provided facts. Otherwise speak qualitatively.",
      "FACTUALITY:FAKE_CITATION": "Do not invent citations, URLs, or named sources.",
      "STRUCTURE:MISSING_FAQ": "Always include an FAQ section.",
      "STRUCTURE:NO_ANSWER_FIRST": "Open with a direct answer containing the target keyword in the first 80 words.",
      "HUMANNESS:AI_ISMS": "Avoid AI-isms (leverage, dive into, in today's world, it's worth noting).",
      "HUMANNESS:LOW_BURSTINESS": "Vary sentence length sharply — mix short punchy lines with longer ones.",
      "CHANNEL:WEAK_HOOK": "Lead with a hook that lands in the first 3 seconds.",
      "CHANNEL:NO_CTA": "End with a clear, natural call to action.",
    };
    return rows.map(r => MAP[r.errorType]).filter(Boolean);
  }

  // Store a defect-free high performer as a few-shot exemplar (ties into your
  // embedding/generation memory — retrieved for similar future briefs).
  private async captureExemplar(
    _teamId: number, _contentType: string, _contentId: number, _content: string
  ): Promise<void> {
    // TODO: embed (topic + content) and upsert into a generationMemory table,
    // then retrieve top-k similar exemplars at generation time as few-shot.
  }
}

export const contentReviewService = ContentReviewService.getInstance();

/* ----------------------------------------------------------------------------
NEW SCHEMA you'll need (Drizzle):

contentReviews:
  id, teamId, contentType, articleId?, socialPostId?, videoIdeaId?,
  dimensionScoresJson (jsonb), defectsJson (jsonb), passed (int), reviewedAt

patternDimensionStats:
  id, patternId, dimension (text), successes (int), trials (int),
  wilsonScore (int), updatedAt
  -- unique index on (patternId, dimension)

In LearningService.buildOptimizationContext, rank patterns by the relevant
dimension's wilsonScore instead of raw successRate, and append
contentReviewService.getNegativeConstraints(...) to promptEnhancements.
---------------------------------------------------------------------------- */